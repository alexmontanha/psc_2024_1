programa {
  funcao inicio() {
    inteiro A
    inteiro B
    inteiro C
    inteiro D

    leia(A)
    leia(B)
    leia(C)
    leia(D)

    inteiro DIFERENCA = (A * B - C * D)

    escreva("DIFERENÇA = ", DIFERENCA)
  }
}
